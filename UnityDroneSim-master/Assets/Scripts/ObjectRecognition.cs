using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectRecognition : MonoBehaviour
{
    void getWorldCoords() {
        projectCoords(getImgCoords());

    }

    void projectCoords(double[] imgCoords) { 
        // convert img coordinates to world coordinates
    }
    double[] getImgCoords() {
        return null;
    }




    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
